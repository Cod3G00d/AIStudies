# Local Ollama — no API key required
# Make sure the Ollama app is running and these models exist:
#   ollama pull llama3.2
#   ollama pull nomic-embed-text
OLLAMA_MODEL=llama3.2
OLLAMA_EMBED_MODEL=nomic-embed-text
